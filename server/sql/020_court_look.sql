-- Внешность корта: цвет покрытия и особенности («Ультраширокий», «Одиночный»).
--
-- Заказчик: у каждого корта своё название, цвет и особенность, и всё это
-- должно задаваться в админке. Какой корт какого цвета — знает только клуб,
-- поэтому по умолчанию пусто: приложение ничего не показывает, пока клуб
-- не заполнит.
--
-- Только добавление колонок: старые версии приложения и сервера их просто
-- не замечают, откат кода базу не ломает.
ALTER TABLE courts ADD COLUMN IF NOT EXISTS color text;
ALTER TABLE courts ADD COLUMN IF NOT EXISTS tags text[] NOT NULL DEFAULT '{}';

-- Что входит в бронь. Заказчик: «в бронь входит только корт, мячи и ракетки
-- оплачиваются отдельно». Строка на панели брони; пусто — текст по умолчанию
-- в приложении, менеджер может переписать его в админке.
ALTER TABLE settings ADD COLUMN IF NOT EXISTS booking_note text;
