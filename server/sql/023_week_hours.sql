-- Часы работы на каждый день недели.
--
-- Раньше была одна пара «открытие — закрытие» на все дни. Заказчик попросил
-- настраивать каждый день отдельно, в том числе ставить выходной.
-- Массив из 7 элементов, с понедельника: {"open":9,"close":24,"closed":false}.
-- Пусто — все дни работают по open_hour и close_hour, как раньше.
-- open_hour и close_hour остаются: в них самое раннее открытие и самое
-- позднее закрытие недели — по ним строятся отчёты и границы особых цен.
ALTER TABLE settings ADD COLUMN IF NOT EXISTS week_hours jsonb;
