-- Новое состояние брони: время удержания вышло.
-- Отдельным файлом, потому что PostgreSQL не даёт использовать
-- только что добавленное значение перечисления в той же транзакции.
ALTER TYPE booking_status ADD VALUE IF NOT EXISTS 'expired';
