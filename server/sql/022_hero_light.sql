-- Фото первого экрана для светлой темы.
--
-- Клубные снимки тёмные: на белом фоне светлой темы такой кадр выглядит
-- чужеродно. Поэтому у главного экрана два фото — для тёмной темы и для
-- светлой. Пусто — приложение показывает встроенное.
ALTER TABLE settings ADD COLUMN IF NOT EXISTS hero_light_url text;
